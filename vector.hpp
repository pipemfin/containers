#ifndef VECTOR
#define VECTOR

namespace ft {

    template<typename T>//, class Alloc = allocator<T>>
    class vector {
    private:
        size_t size;
        size_t capacity;
    public:
        vector();
    };
}

#endif //CONTAINERS_VECTOR_H
